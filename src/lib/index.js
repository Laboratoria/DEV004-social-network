/* const ROUTES = {};

export const onNavigate = (pathname) => {
  const path = ROUTES[pathname] !== 'function' ? pathname : '/';
  window.history.pushState({}, path, window.location.origin + pathname);
};
 */
